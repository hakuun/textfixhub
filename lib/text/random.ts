/**
 * Shared random utilities used by generators.
 *
 * Extracted to avoid duplicating mulberry32 and Fisher-Yates shuffle
 * across generate-sentences.ts and generate-nouns.ts.
 */

/** Simple seeded PRNG (mulberry32). Produces deterministic output for a given seed. */
export function mulberry32(seed: number): () => number {
  return () => {
    seed |= 0;
    seed = (seed + 0x6d2b79f5) | 0;
    let t = Math.imul(seed ^ (seed >>> 15), 1 | seed);
    t = (t + Math.imul(t ^ (t >>> 7), 61 | t)) ^ t;
    return ((t ^ (t >>> 14)) >>> 0) / 4294967296;
  };
}

/** Fisher-Yates shuffle with provided PRNG. Returns a new array. */
export function shuffle<T>(arr: T[], rand: () => number): T[] {
  const result = [...arr];
  for (let i = result.length - 1; i > 0; i--) {
    const j = Math.floor(rand() * (i + 1));
    [result[i], result[j]] = [result[j], result[i]];
  }
  return result;
}
